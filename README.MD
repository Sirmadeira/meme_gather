# TODO

# Issue i ran into - When in the server, there is a default keyring that new apps require. Therefore I set the default one with a password. 
# It seems after that we can easily use chromium on our raspberry if not we run into a browser was not opened issue in reddit scraper. 
# Issue i ran into - User agent is super duper important for avoiding identification from reddit, keeping the numbers low also helps
# Issue i ran into - Reddits api called prawn, is truly annoying to access as they kinda of shut it down. So now we just abuse of pydoll and it is stealth mechanics